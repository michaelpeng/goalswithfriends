<div class="container">
  <div class="row">
    <h3>Users</h3>
    <p>Not here? Login to your profile and ask to be listed!</p>
    <div class="column">
      <table class="table">
        <tbody>
          <% @users.each do |user| %>
            <tr>
              <%= render user %>
            </tr>
          <% end %>
        </tbody>
      </table>
    </div>
  </div>
</div>
