<h3>Trainer</h3>
<p>Name: <%= @trainer.name if @trainer.name %></p>
<p>Email: <%= @trainer.email if @trainer.email %></p>
<p>Pokemons:</p>
  <ul>
  	<% @trainer.pokemons.each do |pokemon| %>
  	  <li><%= pokemon.name %>: Level <%= pokemon.level %>, <%= pokemon.health %> health </li>
  	  <%= button_to "Damage!", damage_path(id: pokemon), :class => "button medium", :method => :patch %>
  	<% end %>
  </ul>


<p> Want more? </p>
<% if @trainer.id == current_trainer.id %>
<%= button_to "Create a Pokemon!", new_pokemon_path, :class => "button medium", :method => :get %>
<% end %>

<% if @trainer.id != current_trainer.id %>
<p> Too bad! Please log in to create a pokemon for <%= @trainer.name %>! </p>
<% end %>